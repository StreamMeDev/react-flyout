/* global describe, it */
var assert = require('assert');
var pkg = require('../');

describe('flyout', function () {
	it('should not error when required, lolz tests', function () {
		assert(pkg);
	});
});
